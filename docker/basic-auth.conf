# Basic Auth fuer die gesamte Seite. Wird vom Entrypoint nur aktiviert,
# wenn BASIC_AUTH_USER und BASIC_AUTH_PASSWORD gesetzt sind.
<Directory "/var/www/html">
    AuthType Basic
    AuthName "LPP Sports"
    AuthUserFile /etc/apache2/.htpasswd
    Require valid-user
</Directory>
