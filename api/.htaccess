# /api/plan  ->  /api/plan.php  (damit die Website nichts ändern muss)
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^plan$ plan.php [L]
</IfModule>

# Konfigurationsdatei mit dem Schlüssel nicht ausliefern
<FilesMatch "^ki-config.*\.php$">
  Require all denied
</FilesMatch>
